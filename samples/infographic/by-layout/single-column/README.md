# Single-column layout samples

This directory holds infographic samples with **single-column** layout (PNG/SVG, etc.).

Add files here and optionally list filenames and short notes (e.g. ratio, topic) in this README.
